using UnityEngine;

public class FuelCap : MonoBehaviour
{
    public MotorcycleFuel bikeFuelSystem;
    public Transform NozzleAttachPoint;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
